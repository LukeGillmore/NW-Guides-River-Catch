import React from 'react';


class AddCatch extends React.Component {
    
    render = () => {
         return (
             <div>
                 This is where you will add catch 
                 three autocomplete dropdowns that must be filled out then a submit button
                 Maybe just make a modal to perform this task
             </div>
         )
    }
}

export default AddCatch; 